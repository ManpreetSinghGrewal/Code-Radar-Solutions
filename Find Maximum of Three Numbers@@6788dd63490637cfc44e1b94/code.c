#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int a,b,c;
    scanf("%d %d %d", &a, &b, &b);
    if(a > b,c){
        printf("%d",a);
    }
    if(b > c,a){
        printf("%d",b);
    }
    if(c > a,b){
        printf("%d",c);
    }
    return 0;
}