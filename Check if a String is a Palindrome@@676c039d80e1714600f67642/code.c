#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char name[40];
    scanf("%s",&name);
    for(int i = 0;arr[i] != '\0';i++){
        printf("%s",name[i]);
    }

    return 0;
}