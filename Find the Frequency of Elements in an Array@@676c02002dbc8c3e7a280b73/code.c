#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int n;
    scanf("%d",&n);
    arr[n];
    for(int i = 0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int freq[n];
    for(int i )
}